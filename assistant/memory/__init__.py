"""Memory: per-contact profiles, standing rules, and SCOPED retrieval.

The retrieval function returns only the context relevant to the current message —
scoped to that contact first — so the prompt never gets a dump of everything.
Stdlib only.
"""
